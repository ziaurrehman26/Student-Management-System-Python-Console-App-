# Student-Management-System-Python-Console-App-
Student Management System is a Python console app to manage student records with CRUD operations (add, update, delete, search). Data is stored in a JSON file for persistence, and all students are shown in a neatly formatted table in the terminal. 
